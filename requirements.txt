# Versioni pinnate per riproducibilità — aggiornare con attenzione
# Aggiornato a 0.86.5 il 2026-08-05 insieme a pyproject.toml (stessa versione
# in entrambi i file, per non farli divergere) — vedi dnd_app/docs/
# changelog_storico.md per il perché.
flet==0.86.5
Pillow>=10.0.0

# Aggiunto il 2026-08-06: ft.FilePicker confermato non funzionante su build
# Android reali (TimeoutException lato Dart, nessuna Activity nativa avviata
# — vedi changelog_storico.md e regole_flet_api.md, sezione FILE PICKER).
# flet-webview è l'estensione ufficiale usata come bypass (ui/
# mobile_webview_picker.py): stessa versione di `flet` per restare in
# lockstep con le release core, come da convenzione di questo file.
flet-webview==0.86.5

# Aggiunto il 2026-08-06: QR d'ingresso per l'hosting LAN — puro Python,
# usa Pillow (già sopra) per generare il PNG. Vedi il commento gemello in
# pyproject.toml e network/qr_join.py.
qrcode==8.2

# Aggiunto il 2026-08-06: terzo tentativo per la selezione immagine su
# Android, dopo il fallimento CONFERMATO sia di ft.FilePicker sia del
# fallback WebView (flet-webview, sopra) — vedi il commento gemello e più
# esteso in pyproject.toml. Estensione Flet scritta su misura per questo
# progetto (non su PyPI), path-based. ⚠️ NON compilata/testata da questo
# sandbox — vedi dnd_app/extensions/flet_image_picker/README.md.
-e ./extensions/flet_image_picker

# Aggiunto il 2026-08-17: stesso principio di flet_image_picker sopra, ma
# per la selezione di un file GENERICO (.dndchar/.dndworld) su Android,
# dopo che anche il fallback WebView è stato confermato non funzionante per
# l'import personaggio/mondo — vedi il commento gemello e più esteso in
# pyproject.toml. ⚠️ NON compilata/testata da questo sandbox — vedi
# dnd_app/extensions/flet_file_picker/README.md.
-e ./extensions/flet_file_picker

# Aggiunto il 2026-08-17: ultimo passo dell'aggiornamento in-app — consegnare
# l'APK scaricato all'installer di pacchetti di SISTEMA su Android. Non
# ottenibile dal Python (serve un FileProvider per l'URI content:// e un intent
# nativo). A differenza delle due estensioni sopra NON contiene codice Kotlin: si
# appoggia al plugin Flutter open_filex, che porta con sé il proprio
# FileProvider. Vedi il commento gemello e più esteso in pyproject.toml.
# ⚠️ NON compilata/testata da questo sandbox — vedi
# dnd_app/extensions/flet_apk_installer/README.md, che elenca i quattro punti da
# verificare (in particolare i vincoli di versione su pub.dev: esiste già un
# precedente di CI rotta su tutte e 4 le piattaforme con flet_file_picker).
-e ./extensions/flet_apk_installer
